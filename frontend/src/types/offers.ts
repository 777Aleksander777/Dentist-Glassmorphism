import { ImageProps } from "./image"

export type OffersOneProps = {
    data:  [
            id:{
                title: string,
                desc: string,
                img: ImageProps,
            }
    ]
    
}